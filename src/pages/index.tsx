import React from 'react';
import styled from 'styled-components';

const IndexPage = styled.div`
  background-color: white;
  min-height: 100vh;
`;

export default function Index(): JSX.Element {
  return <IndexPage>MAIN PAGE</IndexPage>;
}
